{
  "name": "lambda-handler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">=14.18.2 <15",
   "npm": ">=8.19.2 <9"
  },
  "scripts": {
    "build": "PATH=$PATH:../../node_modules/.bin tsc --noEmitOnError",
    "test": "PATH=$PATH:../../node_modules/.bin jest",
    "eslint": "PATH=$PATH:../../node_modules/.bin eslint",
    "lint": "PATH=$PATH:../../node_modules/.bin eslint '**/*.ts' --fix",
    "lint-check": "PATH=$PATH:../../node_modules/.bin eslint '**/*.ts' --max-warnings=0",
    "tsc": "PATH=$PATH:../../node_modules/.bin tsc"
  },
  "author": "",
  "license": "UNLICENSED",
  "private": true
}
